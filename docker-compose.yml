services:
  feedlite:
    container_name: feedlite
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./data:/app/data
      - ./config.yml:/app/config.yml
    env_file:
      - .env
    restart: unless-stopped
